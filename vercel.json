{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/[...path].js": {
      "maxDuration": 60,
      "includeFiles": "server/src/{prompts/*.txt,trees/*.json}"
    }
  }
}
