import PatientIntake from './pages/PatientIntake.jsx';

// Phase 1: patient intake only. Doctor view + routing (react-router-dom) arrive with the dashboard (Day 6).
export default function App() {
  return <PatientIntake />;
}
